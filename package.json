{
  "name": "xvyz",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --ip 0.0.0.0 --port 3000 --live-reload",
    "format": "prettier --write ."
  },
  "dependencies": {
    "prettier": "^3.8.3",
    "prettier-plugin-organize-imports": "^4.3.0",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "wrangler": "^4.83.0"
  }
}
